// * File schema database: prisma/schema.prisma
// & This schema defines identity, RBAC, HR, attendance, submission, and audit domains.
// % Schema ini mendefinisikan domain identitas, RBAC, HR, absensi, pengajuan, dan audit.

// & Prisma client generators used by backend runtime and validation types.
// % Generator Prisma client yang dipakai runtime backend dan tipe validasi.

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

generator prismabox {
  provider                    = "prismabox"
  typeboxImportDependencyName = "elysia"
  typeboxImportVariableName   = "t"
  additionalProperties        = true
  inputModel                  = true
  output                      = "../src/generated/prismabox"
}

datasource db {
  provider = "postgresql"
}

// & Shared enums used by RBAC and attendance-related models.
// % Enum bersama yang dipakai model RBAC dan model terkait absensi.

enum PermissionAction {
  CREATE
  READ
  UPDATE
  DELETE
  APPROVE
}

enum AttendanceStatus {
  PRESENT // Hadir tepat waktu
  LATE // Telat
  ABSENT // Alpa / Gak masuk
  LEAVE // Izin / Cuti
  OFF // Memang jadwalnya libur
  INCOMPLETE // Data absensi gak lengkap (misal: check-in ada tapi check-out gak ada)
}

// & Core identity and RBAC authorization models.
// % Model inti identitas user dan otorisasi RBAC.

model Users {
  id        String   @id @default(uuid())
  nip       String   @unique
  password  String
  rbacRoleId String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // & Point wallet denormalized fields for performance
  // % Field integritas dompet untuk performa
  currentPoints   Int     @default(0)

  rbacRole RbacRoles? @relation(fields: [rbacRoleId], references: [id])

  managedDivisions Divisions[]   @relation("DivisionManager")
  evaluationsGiven Assessments[] @relation("EvaluatorRelation")

  employees   Employees?
  
  userFaces   UserFaces?
  submissions Submissions[]
  manualEntryAttendances Attendances[] @relation("ManualEntryBy")
  
  // & Point wallet relations
  // % Relasi dompet integritas
  pointLedgers PointLedgers[]
  userTokens   UserTokens[]

  // & Notifications relations
  // % Relasi untuk sistem notifikasi
  notifications Notifications[]
  pushSubscriptions PushSubscriptions[]

  // & Ticketing relations
  reportedTickets  Tickets[]         @relation("TicketReporter")
  operatedTickets  Tickets[]         @relation("TicketOperator")
  ticketResponses  TicketResponses[] @relation("TicketResponder")
}

// & Gamification models for badges and points.
// % Model gamifikasi untuk badge dan poin.
enum NotificationCategory {
  ATTENDANCE      // Notif absensi (check-in/out berhasil, absen)
  POINTS          // Riwayat poin (earn, penalty, adjustment)
  SCHEDULE        // Perubahan jadwal kerja
  ASSESSMENT      // Dinilai oleh supervisor/evaluator
  SUBMISSION      // Pengajuan disetujui/ditolak
  GENERAL         // Lainnya
}

model Notifications {
  id          String               @id @default(uuid())
  userId      String
  user        Users                @relation(fields: [userId], references: [id], onDelete: Cascade)

  title       String
  body        String               @db.Text
  category    NotificationCategory @default(GENERAL)
  isRead      Boolean              @default(false)
  readAt      DateTime?

  // Referensi opsional ke entitas terkait
  referenceEntity String?          // "attendance", "submission", "assessment", etc.
  referenceId     String?          // UUID entity terkait

  // Web Push metadata
  pushSent    Boolean              @default(false)
  pushSentAt  DateTime?

  createdAt   DateTime             @default(now())
  updatedAt   DateTime             @updatedAt

  @@index([userId, isRead, createdAt])
  @@index([userId, category])
}

model PushSubscriptions {
  id         String   @id @default(uuid())
  userId     String
  user       Users    @relation(fields: [userId], references: [id], onDelete: Cascade)
  endpoint   String   @unique
  p256dh     String
  auth       String
  userAgent  String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  
  @@index([userId])
}

model RbacRoles {
  id        String   @id @default(uuid())
  key       String   @unique
  name      String
  isSystem  Boolean  @default(false)
  isActive  Boolean  @default(true)
  canAccessAdmin Boolean @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  users       Users[]
  permissions RolePermissionActions[]
}

model PermissionResources {
  id              String   @id @default(uuid())
  key             String   @unique
  name            String
  routePath       String?
  groupName       String?
  supportsApprove Boolean  @default(false)
  isActive        Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  permissions RolePermissionActions[]
}

model RolePermissionActions {
  id         String           @id @default(uuid())
  roleId     String
  resourceId String
  action     PermissionAction
  isAllowed  Boolean          @default(false)
  createdAt  DateTime         @default(now())
  updatedAt  DateTime         @updatedAt

  role     RbacRoles           @relation(fields: [roleId], references: [id], onDelete: Cascade)
  resource PermissionResources @relation(fields: [resourceId], references: [id], onDelete: Cascade)

  @@unique([roleId, resourceId, action])
  @@index([roleId])
  @@index([resourceId])
}

model Positions {
  id        String @id @default(uuid())
  name      String
  gajiPokok Int

  // Untuk menentukan level akses (e.g., Supervisor ke atas)
  isManagerial Boolean @default(false)

  // gajiPokok Decimal     @db.Decimal(15, 2)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  //? satu divisi
  divisionId String?
  division   Divisions? @relation(fields: [divisionId], references: [id])

  employees Employees[]
}

model Divisions {
  id          String  @id @default(uuid())
  name        String
  description String?

  managerId String? @unique
  manager   Users?  @relation("DivisionManager", fields: [managerId], references: [id])

  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt
  positions Positions[]
}

model Employees {
  id       String  @id @default(uuid())
  fullName String
  address  String?

  email       String? @unique
  phoneNumber String? @unique

  joinDate DateTime @default(now())

  userId String @unique
  user   Users  @relation(references: [id], fields: [userId])

  positionId String?
  position   Positions? @relation(references: [id], fields: [positionId])

  // scheduleId String?
  // schedule   Schedules? @relation(references: [id], fields: [scheduleId])

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  evaluationsReceived Assessments[] @relation("EvaluateeRelation")

  employeeDetails    EmployeeDetails[]
  attendances        Attendances[]
  // shiftSchedulePivots     ShiftSchedulePivot[]
  workingSchedules   WorkingSchedules? @relation(fields: [workingSchedulesId], references: [id])
  workingSchedulesId String?
}


model EmployeeDetails {
  employeeId        String    @id
  employee          Employees @relation(fields: [employeeId], references: [id])
  dateOfBirth       DateTime? @db.Date
  placeOfBirth      String?
  gender            String?
  maritalStatus     String?
  religion          String?
  profilePictureUrl String?
  bankName          String?
  bankAccountNumber String?
  hireDate          DateTime? @db.Date
  employmentType    String?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt
}

// & Assessment performance models used by evaluator workflow.
// % Model penilaian kinerja yang dipakai alur evaluator.

model AssessmentCategories {
  id          String  @id @default(uuid())
  name        String // "Kerja Sama", "Inisiatif", "Kedisiplinan"
  description String? @db.Text
  // type        String? // Buat filter: "Employee", "Managerial"
  isActive    Boolean @default(true)

  // Fitur Rahasia (Req #4): Admin bisa nentuin nilai ini boleh diliat bawahan atau nggak
  isVisibleToEmployee Boolean @default(true)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  assessmentDetails AssessmentDetails[]
}

model Assessments {
  id String @id @default(uuid())

  // Siapa yang menilai (Manager / HRD) -> Relasi ke tabel Users
  evaluatorId String
  evaluator   Users  @relation("EvaluatorRelation", fields: [evaluatorId], references: [id])

  // Siapa yang dinilai (Bawahan / Staff) -> Relasi ke tabel Employees
  evaluateeId String
  evaluatee   Employees @relation("EvaluateeRelation", fields: [evaluateeId], references: [id])

  assessmentDate DateTime @db.Date
  period         String // "Minggu 1 Jan 2026", "Q1 2026", "Semester 1"
  generalNotes   String?  @db.Text // Kesimpulan / Feedback text

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  details AssessmentDetails[]
}

model AssessmentDetails {
  id String @id @default(uuid())

  assessmentId String
  assessment   Assessments @relation(fields: [assessmentId], references: [id], onDelete: Cascade)

  categoryId String
  category   AssessmentCategories @relation(fields: [categoryId], references: [id])

  categoryName String // Snapshot nama kategori saat penilaian dibuat (biar gak berubah walaupun kategori diubah kemudian)

  // Pake Decimal biar bisa ngasih nilai 4.5 atau 8.5
  score Decimal @db.Decimal(5, 2)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Biar 1 form penilaian gak ada kategori yang dobel/diisi dua kali
  @@unique([assessmentId, categoryId])
}

// & Geofence model used as attendance location boundary source.
// % Model geofence yang dipakai sebagai sumber batas lokasi absensi.
model Geofences {
  id                  String        @id @default(uuid())
  name                String
  latitude            Decimal       @db.Decimal(10, 7)
  longitude           Decimal       @db.Decimal(10, 7)
  radius              Int // dalam meter
  createdAt           DateTime      @default(now())
  updatedAt           DateTime      @updatedAt
  attendancesCheckIn  Attendances[] @relation("AttendanceCheckIn")
  attendancesCheckOut Attendances[] @relation("AttendanceCheckOut")
}

// & Enum dan model pengajuan yang dipakai alur request karyawan.
enum SubmissionType {
  IZIN_SAKIT
  IZIN_KHUSUS
  DINAS_LUAR
}

enum SubmissionStatus {
  PENDING // Menunggu (Warna Kuning/Abu)
  APPROVED // Disetujui (Warna Hijau)
  REJECTED // Ditolak (Warna Merah)
}

// & PENGAJUAN
model Submissions {
  id     String @id @default(uuid())
  userId String

  // Data dari Form Karyawan
  type       SubmissionType // Dropdown "Tipe Pengajuan"
  startDate  DateTime // "Tanggal Mulai"
  endDate    DateTime // "Tanggal Selesai"
  reason     String         @db.Text // Text Area "Alasan"
  attachment String? // URL file lampiran di Cloudinary
  attachmentPublicId String? // public_id Cloudinary untuk kebutuhan cleanup saat delete
  attachmentOriginalName String?
  attachmentMimeType String?
  attachmentSizeBytes Int?

  // Data dari HRD / Sistem
  status          SubmissionStatus @default(PENDING) // Status awal selalu PENDING
  rejectionReason String?          @db.Text // Alasan kalau ditolak HRD

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relasi ke tabel Users yang udah lu punya
  user Users @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// & Work schedule models for shift templates and per-day schedule mapping.
// % Model jadwal kerja untuk template shift dan mapping jadwal per hari.

model Shifts {
  id        String @id @default(uuid())
  name      String // Misal: "Shift Pagi", "Normal Day"
  startTime String // "08:00"
  endTime   String // "17:00"

  isCrossDay Boolean  @default(false) // Untuk handle shift yang melewati tengah malam
  createdAt  DateTime @default(now())

  scheduleDays ScheduleDays[]
}

model WorkingSchedules {
  id        String   @id @default(uuid())
  name      String // Misal: "Jadwal Operasional Kantor"
  createdAt DateTime @default(now())

  days      ScheduleDays[]
  // shifts    Shifts[]
  employees Employees[]
}

model ScheduleDays {
  id        String  @id @default(uuid())
  dayOfWeek String // "Monday", "Tuesday", dsb
  isActive  Boolean @default(true) // Untuk handle toggle Enable/Disable di UI

  // Relasi ke Master Shift
  shiftId String? // Nullable kalau isActive: false
  shift   Shifts? @relation(fields: [shiftId], references: [id])

  workingScheduleId String
  workingSchedule   WorkingSchedules @relation(fields: [workingScheduleId], references: [id], onDelete: Cascade)

  @@unique([workingScheduleId, dayOfWeek])
}

model PublicHolidays {
  id        String   @id @default(uuid())
  name      String
  date      DateTime @unique @db.Date
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// & Attendance and audit models for presence records and traceability.
// % Model absensi dan audit untuk pencatatan kehadiran serta jejak perubahan.
model Attendances {
  id         String    @id @default(uuid())
  employeeId String
  employee   Employees @relation(fields: [employeeId], references: [id])

  shiftNameSnapshot        String // Misal: "Pagi"
  expectedCheckInSnapshot  DateTime // Jam masuk yang seharusnya (diambil dari Shift saat itu)
  expectedCheckOutSnapshot DateTime? // Jam pulang yang seharusnya

  // // DATA SNAPSHOT (Waktu sesuai jadwal)
  // shiftStartTimeSnapshot DateTime  @db.Time() // Jam mulai shift (snapshot)
  // shiftEndTimeSnapshot   DateTime? @db.Time() // Jam selesai shift (snapshot)

  // REAL DATA (Waktu aslinya)
  checkIn  DateTime?
  checkOut DateTime?

  isManualEntry    Boolean   @default(false) // tandain ini manual atau otomatis
  manualReason    String? // "Forgot clock-in", "Technical issue", dll
  manualNotes     String?   @db.Text // Additional notes dari UI
  manualEntryBy   String? // userId admin yang input
  manualEntryAt   DateTime? // kapan admin input ini
  manualEntryByRole String? // snapshot role admin saat entry dilakukan

  // Relasi ke admin yang input
  manualEntryUser Users? @relation("ManualEntryBy", fields: [manualEntryBy], references: [id])

  // Lokasi absensi (nama tempat)
  deviceInfo String? @db.Text

  // Metode absensi: FACE_RECOGNITION 
  checkInPhoto  String? @db.Text
  checkOutPhoto String? @db.Text

  // Status absensi: PRESENT, LATE, ABSENT, LEAVE, OFF
  status         AttendanceStatus  @default(OFF)
  statusCheckOut AttendanceStatus? // Status saat check out (bisa null kalau belum check out)

  latitudeCheckInSnapshot  Decimal? @db.Decimal(10, 7) // lokasi absensi check in
  longitudeCheckInSnapshot Decimal? @db.Decimal(10, 7) // lokasi absensi check in

  latitudeCheckOutSnapshot  Decimal? @db.Decimal(10, 7) // lokasi absensi check out
  longitudeCheckOutSnapshot Decimal? @db.Decimal(10, 7) // lokasi absensi check out

  radiusCheckInSnapshot  Int? // radius geofence saat check-in
  radiusCheckOutSnapshot Int? // radius geofence saat check-out

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  geofencesId String?

  geofences Geofences? @relation("AttendanceCheckIn", fields: [geofencesId], references: [id])

  geofencesCheckOutId String?

  geofencesCheckOut Geofences? @relation("AttendanceCheckOut", fields: [geofencesCheckOutId], references: [id])

  // & Point wallet integration fields
  // % Field integrasi dompet integritas
  pointsEarned    Int?     // Poin yang diperoleh dari absensi ini
  usedTokenId     String?  @unique // FK ke UserTokens.id (must be unique for one-to-one)
  usedToken       UserTokens? @relation("AttendanceUsedToken", fields: [usedTokenId], references: [id], onDelete: SetNull)
}

model AuditLogs {
  id        String   @id @default(uuid())
  action    String // Misal: "UPDATE_ATTENDANCE_MANUAL"
  entity    String // Misal: "Attendances"
  entityId  String // ID dari data absensi yang diedit
  userId    String // ID si Admin HRD yang ngedit
  userRole  String // Misal: "ADMIN"
  changes   Json // Nyimpen data sebelum & sesudah diedit { before: {...}, after: {...} }
  reason    String? // Alasan ngedit: "HP karyawan mati, diwakilin"
  createdAt DateTime @default(now())
}

model UserFaces {
  id     String @id @default(uuid())
  userId String @unique
  user   Users  @relation(fields: [userId], references: [id])

  // Data wajah dalam bentuk Binary (BLOB/ByteA)
  faceData Bytes @db.ByteA

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}



// & POINT WALLET (DOMPET INTEGRITAS) MODELS
// & Dynamic Rule Engine, Transaction Ledger, Marketplace Catalog, Token Inventory
// % Model Sistem Dompet Integritas: Mesin Aturan Dinamis, Buku Besar, Katalog Marketplace, Inventory Token

// & Point Rules: Dynamic rule engine for automated point calculation
// % Aturan Poin: Mesin aturan dinamis untuk kalkulasi poin otomatis
model PointRules {
  id              String   @id @default(uuid())
  ruleName        String   // "Datang Pagi Banget", "Terlambat 15+ Menit"
  targetRole      String   // "USER", "ADMIN", "HR" (dari RbacRoles.key)
  
  // & Condition: JIKA jam_check_in < 06:30 MAKA +5
  // % Kondisi: IF jam_check_in < 06:30 THEN +5
  conditionField  String   // "check_in_time" | "late_minutes" | "attendance_status"
  conditionOp     String   // "<", ">", "==", "between", "in"
  conditionValue  String   // "06:30:00" | "15" | "LATE"
  
  pointModifier   Int      // +5, -3, 0 (dapat positif atau negatif)
  description     String?  @db.Text
  isActive        Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  @@index([isActive, targetRole])
}


// & Enum untuk Tipe Transaksi Poin
// % Enum for Point Transaction Types
enum TransactionType {
  EARN
  SPEND
  PENALTY
  ADJUSTMENT
}

// & Point Ledgers: Audit trail / bank-style transaction tracking for all point mutations
// % Buku Besar Poin: Jejak audit / pencatatan transaksi ala bank untuk semua mutasi poin
model PointLedgers {
  id              String   @id @default(uuid())
  userId          String
  user            Users    @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  transactionType TransactionType
  amount          Int      // +5, -3 (signed)
  balanceBefore   Int      // Saldo sebelum transaksi
  balanceAfter    Int      // Saldo sesudah transaksi
  
  description     String?  @db.Text // "Check-in tepat waktu 12/08/2024"
  referenceEntity String?  // "attendance", "token_purchase", "rule_trigger"
  referenceId     String?  // FK ke attendance.id atau transaction lain
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  @@index([userId, createdAt])
  @@index([transactionType])
}

// & Flexibility Items: Marketplace catalog for tradeable rewards (tokens)
// % Item Fleksibilitas: Katalog marketplace untuk reward yang bisa ditukar (token)
model FlexibilityItems {
  id              String   @id @default(uuid())
  itemName        String   // "Bebas Terlambat 15 Menit", "Izin Tanpa Surat"
  pointCost       Int      // 20, 50, 100
  
  itemType        String   // "late_allowance", "absence_excuse", "wfh", "overtime"
  durationDays    Int      // 1, 7, 30 (masa berlaku token)
  maxPerMonth     Int?     // Stock limit per user per bulan (nullable = unlimited)
  conditionField  String?  // "attendance.status" | "attendance.lateMinutes"
  conditionValue  String?  // "ABSENT" | "30"
  expiredAt       DateTime? // Masa aktif listing item di marketplace
  
  description     String?  @db.Text
  iconUrl         String?  // URL ilustrasi dari cloudinary
  
  isActive        Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  userTokens      UserTokens[]

  @@index([isActive, expiredAt])
}

// & User Tokens: User inventory of purchased flexibility tokens
// % Token Pengguna: Inventory token fleksibilitas yang dibeli pengguna
model UserTokens {
  id              String   @id @default(uuid())
  userId          String
  user            Users    @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  itemId          String
  item            FlexibilityItems @relation(fields: [itemId], references: [id], onDelete: Restrict)
  
  status          String   // "AVAILABLE", "USED", "EXPIRED"
  usedAt          DateTime?        // Kapan token ini digunakan
  
  // & Back-reference to attendance where this token was used
  // % Referensi balik ke absensi dimana token ini dipakai
  attendance      Attendances? @relation("AttendanceUsedToken")
  
  expiresAt       DateTime // Token berlaku sampai kapan
  remainingDays   Int      @default(0) // Cache sisa hari, disinkronkan dari expiresAt
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  @@index([userId, status])
  @@index([expiresAt])
}

// ──────────────────────────────────────────────────────────────
// & TICKETING SYSTEM
// % Sistem manajemen tiket helpdesk
// ──────────────────────────────────────────────────────────────

enum TicketPriority {
  LOW
  MEDIUM
  HIGH
}

enum TicketStatus {
  OPEN
  IN_PROGRESS
  CLOSED
}

model Tickets {
  id          String         @id @default(uuid())
  reporterId  String
  reporter    Users          @relation("TicketReporter", fields: [reporterId], references: [id], onDelete: Cascade)

  operatorId  String?
  operator    Users?         @relation("TicketOperator", fields: [operatorId], references: [id], onDelete: SetNull)

  subject     String
  description String         @db.Text
  autoReplyText String?      @db.Text

  priority    TicketPriority @default(MEDIUM)
  status      TicketStatus   @default(OPEN)

  firstResponseAt DateTime?
  closedAt        DateTime?

  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt

  responses         TicketResponses[]
  satisfactionRating SatisfactionRatings?

  @@index([reporterId, status])
  @@index([operatorId, status])
  @@index([status, priority])
}

model TicketResponses {
  id          String   @id @default(uuid())
  ticketId    String
  ticket      Tickets  @relation(fields: [ticketId], references: [id], onDelete: Cascade)

  responderId String
  responder   Users    @relation("TicketResponder", fields: [responderId], references: [id], onDelete: Cascade)

  message     String   @db.Text
  isAutoReply Boolean  @default(false)

  createdAt   DateTime @default(now())

  @@index([ticketId, createdAt])
}

model SatisfactionRatings {
  id        String   @id @default(uuid())
  ticketId  String   @unique
  ticket    Tickets  @relation(fields: [ticketId], references: [id], onDelete: Cascade)

  score     Int      // 1-5
  feedback  String?  @db.Text

  createdAt DateTime @default(now())
}
